var express = require('express');
var fs = require('fs');

var app = express.createServer(express. app.get('/', function(request, response) {
    
    var dt=data.toString("utf-8",0,27);
    response.send( });
});
var port = process.env.PORT || 5000;
app.listen( console.log("Listening on " + port);
});
port, function() {
dt);
logger());
 fs. readFile(' index. 
html', if ( err) throw err;
function ( err, data){
